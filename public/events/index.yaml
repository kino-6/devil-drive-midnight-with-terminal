# Event config split-loader entrypoint.
# Event pools feed NAVI route briefings and route-choice candidates.
version: "events-v1"
includes: "/events/route_events.yaml, /events/salvage_events.yaml, /events/anomaly_events.yaml, /events/boss_events.yaml, /events/return_events.yaml"
